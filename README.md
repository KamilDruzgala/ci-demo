# CI Demo

Demonstracyjny projekt CI w GitHub Actions z konteneryzacją w Dockerze.

Praca inżynierska: *Automatyzacja budowania, testowania oraz oceny jakości aplikacji 
z wykorzystaniem Docker i GitHub Actions*

## Uruchomienie lokalne

​```bash
docker build -t ci-demo .
docker run --rm ci-demo
​```